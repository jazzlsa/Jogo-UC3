# Ferramentas de autoria (extração de material + geração de casos com Gemini).
# Roda só localmente, na máquina de quem gera conteúdo — nunca é empacotada
# junto do jogo distribuído (app/ e mobile/).
gspread>=6.0.0
google-auth>=2.28.0
notebooklm-py>=0.8.1
google-genai>=0.1.1
httpx>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
